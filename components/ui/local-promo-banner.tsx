'use client';

interface LocalPromoBannerProps {
  zips?: string[];
  endDate?: string;
}

export default function LocalPromoBanner(_props: LocalPromoBannerProps) {
  // Temporarily disabled while keeping call sites untouched.
  return null;
}
